PORT=3002
JWT_SECRET=change_me_to_a_long_random_secret
FRONTEND_ORIGIN=http://localhost:5173

# PostgreSQL connection string (libpqxx / libpq format).
# Local dev:   psql -U postgres -d vprasalnik -f backend/vprasalnik.sql
# Docker:      handled automatically via docker-compose (cicdq database)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vprasalnik
